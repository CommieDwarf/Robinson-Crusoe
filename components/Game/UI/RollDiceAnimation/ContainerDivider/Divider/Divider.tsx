// @flow
import * as React from 'react';

type Props = {
    onMouseEnter: () => void;
    onMouseLeave: () => void;
};
export const Divider = (props: Props) => {
    return (
        <div>

        </div>
    );
};