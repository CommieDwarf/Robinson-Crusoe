// @flow
import * as React from 'react';
import styles from "./MysteryResolve.module.css";

type Props = {};
export const MysteryResolve = (props: Props) => {
    return (
        <div className={styles.container}>

        </div>
    );
};